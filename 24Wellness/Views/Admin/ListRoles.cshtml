@model IEnumerable<IdentityEntityFrameworkBuilderExtensions>
@{
    ViewBag.Title = "All Roles";
}
<h1>All Roles</h1>

@if (Model.Any())
{

}
else
{
    <div class="card">
    <div class = "card-header">
            No roles Created Yet
    </div>
    <div class="card-body">
        <h5 class="card-title">
            Use the button below to create a role
        </h5>
        <a class="btn btn-primary" style="width:auto">
            <asp-controller = "View"> </asp-controller> 
            <asp-action> = "CreateRole"</asp-action>
            Create Role
        </a>
    </div>
    </div>
}